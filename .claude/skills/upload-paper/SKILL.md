---
name: upload-paper
description: Add a new publication to the GLADIA website. Interactively collects all paper metadata (title, authors, venue, abstract, links, BibTeX, optional featured image) one field at a time, then creates content/publication/<slug>/index.md, cite.bib, and optionally drops in a featured.png. Use when the user says they want to "add a paper", "upload a paper", "add a publication", or similar.
---

# upload-paper (Claude Code skill wrapper)

The canonical, tool-neutral workflow lives at [`agents/upload-paper.md`](../../../agents/upload-paper.md) in the repo root.

Read that file and follow it exactly. Do not duplicate or summarize it here — it is the single source of truth so all AI tools (Claude Code, Cursor, Codex, …) stay in sync.
